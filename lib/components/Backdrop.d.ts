import { ReactNode } from 'react';
type Props = {
    className?: string;
    children: ReactNode;
    onClick?: () => void;
};
export declare const Backdrop: ({ className, children, onClick }: Props) => import("react/jsx-runtime").JSX.Element;
export {};
